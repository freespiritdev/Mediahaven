<%= simple_form_for([@tune, @reviewtune]) do |f| %>
  <% if @reviewtune.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reviewtune.errors.count, "error") %> prohibited this review from being saved:</h2>
      
      <ul>
      <% @reviewtune.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" id="form">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit "Submit", class: 'btn btn-info'  %>
  </div>
<% end %>


