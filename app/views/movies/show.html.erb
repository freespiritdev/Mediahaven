<h1><%= @movie.title %></h1>
<%= link_to "Review it", new_movie_review_path(@movie), class: "btn btn-info" %>
<%= link_to "Edit", edit_movie_path(@movie) %> |
<%= link_to 'Back', movies_path %>

<div class="row">
  <div class="col-md-5">
    
  </div>

  <div class="col-md-4 col-md-offset-1">
    <h1 class="review-title">Reviews</h1>
    <% if @reviews.blank? %>
      <h3>No reviews yet, go ahead and become the first to review this movie!</h3>
    <% else %>
      <% @reviews.each do |review| %>
        <div class="reviews">
          <p class="col-md-8 col-md-offset-3"><%= review.comment %></p>
        </div>
      <% end %>
    <% end %>
  </div>
</div>



 
