<h1>Edit Your TV Show</h1>

<%= link_to 'Show', @serie %> |
<%= link_to 'Back', series_path %>

<%= simple_form_for @serie, html: { :multipart => true } do |f| %>
 <div class="form-inputs">
  <%= f.label :title %>
  <%= f.text_field :title %> 
  <%= f.submit "Update", class: 'btn btn-info'  %>
</div>
<% end %>